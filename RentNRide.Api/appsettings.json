{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "RabbitMQ": {
    "Host": "${RABBITMQ_HOST}",
    "Port": "${RABBITMQ_PORT}",
    "User": "${RABBITMQ_USER}",
    "Pass": "${RABBITMQ_PASSWORD}"
  },
  "ConnectionStrings": {
    "Postgres": "Host=${POSTGRESQL_HOST};Port=${POSTGRESQL_PORT};Database=${POSTGRESQL_DB};Username=${POSTGRESQL_USER};Password=${POSTGRESQL_PASSWORD};"
  },
  "Minio": {
    "Host": "${MINIO_HOST}",
    "Port": "${MINIO_PORT}",
    "User": "${MINIO_USER}",
    "Password": "${MINIO_PASSWORD}"
  }

}
